# 变更记录

## 0.7.1

校准对齐：把"这个技能到底干什么、指导什么、核心在哪"钉进开头，并拿它当尺子把工程化收到只剩一处。

- SKILL.md 开头新增五条定位声明（做什么 / 指导什么 / 核心在哪 / 不做什么 / 工程化的边界），后面每一节都按它对齐。
- **删除 `format_typography.py`**：排版整理不属于"最小约束"，改为一句话收尾规则（半角引号配对转中文引号、英文省略号转中文省略号，不成对逐处确认）。
- **工程化只剩 `init_project.py` 一处**：它不判断、不检查、不评分，只把目录与模板摆成约定的形状。
- 取消人格的三值状态机（待验收 / active / frozen）：项目是否就绪由 `memory.md` 的项目状态一处记录，人格文件只留验收结论、试写与转译取舍。
- 修正骨架说明：init 只复制四份状态模板，卷纲 / 幕纲 / 章纲 / 摘要模板按需取用。
- 测试 13 → 5 项，只覆盖建骨架脚本与文档自洽。

## 0.7.0

减负：删掉"伪装工程化"的流程与门禁，回到"它只是一个写作 skill"。技能里不再有检查项、评分和必须通过的关卡。

### 删

- **一致性检查 `check_project.py`**：10 类检查、错误 / 提示 / `--strict` 门禁，连同测试一并删除。结构问题写作时自己看一眼，不靠工具裁决。
- **机械体检 `lint_prose.py`**：228 行正则判 AI 味，连同 T1 / T2 判定输出删除。写后回看清单保留在 `references/anti_ai.md`，由虚拟作家自己判断。
- **幕末归档 `snapshot_state.py` 与 `state/archive/`**：每幕多一个文件、一个脚本、一条检查，实际没人回看；历史由正文和事件流水承担。
- **成稿导出 `export_manuscript.py`、回溯检索 `search_chapters.py`**：与模型自带的读写检索能力重复。
- **状态频率表与固定字段**：事件流水不再要求 7 字段，改为一句话记关键变化；活跃事实不再用 `+` / `-` 增量记号；6 个状态节合并为 4 块。
- **章后对账（每章一次）**：降为幕纲里的一句话规则——因果链是假设，偏离就改。
- **`init_project.py --style-report`**：报告路径登记由写作时手动写入 memory.md。
- **试写验收不再调用上游 `fidelity` 脚本**：客观保真校验属上游能力，需要时由用户单独跑；本技能只做"写几个短场景给用户看"。

### 改

- `SKILL.md`：315 行 → 约 160 行。每章工作流 9 步 → 4 步，定稿后 5 步 → 2 步；设计原则 11 条 → 4 条；删除人格变更协议、报告转译映射表、状态更新频率表、机械体检章节。
- 报告转译映射表移到 `references/style_report_mapping.md`，去AI味纪律移到 `references/anti_ai.md`——只在需要时读，不占每章上下文。
- 模板瘦身：章纲 60 → 30 行（去掉接口三件套与交付前自检），人格去掉独立"创作签名"段，故事圣经的"创作思维"降为可选附录。
- 工具 7 → 2：只留 `init_project.py`（建骨架）和 `format_typography.py`（排版整理，去掉 `--diff` / `--output`）。
- 测试 46 → 13 项，只覆盖剩余两个脚本和文档自洽。

### 为什么

门禁式管理把写作 skill 变成了流程系统：每章要维护 6 个状态节、1 条 7 字段流水、Canon 登记、章后对账、机械体检逐条裁决，写 3000 字正文的流程成本接近甚至超过正文本身。规则越密，越容易被跳过或走形式——过度的规则密度反而降低了规则的实际约束力。技能的重点是文学创作，不是工程管理。

## 0.6.0

内核层迭代：从"文档系统"转向"压力系统"，把技能自己写出的三条直觉（稳定内核不冻结姿态、去AI味靠眼前有事、规划从真实结果生长）变成可执行结构。

### 人格

- 人格分两轨：**不变量层**（每章必读的 3–6 条决策句）+ **姿态层**（场景类型 → 语域映射表）。
- 新增**报告转译映射表**：报告四层（含此前无落位的定量层与思维层）逐字段落位。
- 新增三条转译纪律：证据引文不进人格、定量指标只作节奏参照、答不出"服务哪条读者体验"不入库。
- 新增**人格变更协议**与变更记录段；试写验收对齐上游要求（2–3 场景、每个 ≥800 字、日常/冲突/抉择三类，并用 `fidelity` 做客观校验）。
- 修正优先级链：原"叙述声音"在模板中无对应段落，改为可解析的 `不变量层 > 作家定义 > 风格基调 > 红线 > 姿态层 > 技法卡`。

### 状态

- 更新频率**按用途定、不按幕界定**：活跃事实记增量、软连续性（上限 8 条）、读者状态（上限 10 条）、线索账本、下一章约束全部每章更新；幕末只做合并与归档。
- 事件流水改为**结构化固定字段**，可被脚本解析与对账。
- 新增**线索账本**（埋设章 / 状态 / 计划回收章），超期未处置由检查工具告警。
- 幕末重写前先**归档快照**到 `state/archive/`，重写不再不可逆。

### 设定与规划

- 故事承诺补齐第六项**主题张力**（原模板只有五字段）。
- 新增**主题命题**（命题 / 反命题 / 主角当前答案 / 本卷逼问），主题成为取舍判据；幕摘要新增"主题移动"。
- 新增**Canon 维护回路**：每章定稿后若产生不可逆事件或新硬规则，当场登记，不攒到幕末。
- 章纲新增**输入/输出状态**与**每章最低交付**，场景块补齐"演到哪一步停""哪条信息留到下一场"。
- 幕纲的章节因果链明确为**假设**：新增章后对账、中幕重规划触发与压力线停滞阈值。
- 章纲末尾新增**交付前自检**清单。
- 故事圣经新增**人物声纹**表（可选，多人物对白时填）。

### 去AI味

- 资产拆为**场景纪律**（正向、开写前必过、不可选）与**机械体检**（负向、定稿后只报告、不改写不阻断不评分）。
- T1 由"出现即替换"改为"出现即回看"，保留清单、去掉强制性；误杀防护保留。

### 工具

- `init_project.py`：新增 `--style-report`（只登记路径，不复制报告原文）；新增 `state/archive/`。
- `check_project.py`：新增正文与草稿内容比对、事件流水格式、Canon 来源章、人格结构与验收状态、坐标与最高章号、幕末归档、线索超期、活跃事实来源章检查；新增 `--strict`。
- `format_typography.py`：修复代码块内孤立引号导致整份文件被拒的缺陷（代码块与标题行不再参与引号配对判断）；新增 `--diff`。
- 新增 `lint_prose.py`（机械体检）、`snapshot_state.py`（幕末归档）、`export_manuscript.py`（成稿导出与字数统计）、`search_chapters.py`（回溯检索）。
- 新增 `tests/`：46 项 pytest 用例覆盖全部脚本。

### 工作流

- 补写**四步修订闭环**（受理 → 影响扫描 → 批准与执行 → 复核），使描述承诺与正文一致；此前正文只有事件流水的修订行格式，没有流程。

### 文档

- 重写技能描述：补齐触发词（开新书 / 定题材 / 写大纲 / 写章纲 / 续写下一章 / 整理设定 / 查前后矛盾与伏笔 / 卡文 / 改稿定稿 / 去AI味）与边界声明（不负责风格报告，报告产出属上游技能）；SKILL.md 与 manifest 两处描述逐字一致，且每条能力声明都有正文落位。
- 新增 `CHANGELOG.md`；`manifest.yaml` 登记全部 7 个函数。
- 新增 `assets/examples/persona_translation_example.md`：报告 → 人格的完整转译示范与取舍记录表。
- 明确章号**全书全局递增**且已定稿章号不可回改；明确摘要命名（幕 `volume_VV_act_AA.md` / 卷 `volume_VV_summary.md`）。
- `memory.md` 新增**用户偏好**段与风格报告登记段。
- 同步修正上游 `author-persona-skill` 的契约表述（`--style-report`、创作签名、技法卡段号与数量）。

## 0.5.1

- `current_state.md` 合并为唯一状态文件，取消基线与增量拆分。
- 风格报告一次转译进人格，取消独立转接文件。
- 默认去AI味技法卡改为构建人格时的可选输入。
