---
name: novel-writer
description: 当需要策划、创作、续写或修订中文长篇小说时使用——用户提到开新书、定题材、写故事大纲、写章纲、续写下一章、整理设定人物与世界观、检查前后矛盾与伏笔、卡文、改稿定稿、去AI味时即应触发，无需点名本技能。以虚拟作家人格（不变量层决策句 + 场景姿态层）、故事承诺与主题命题、卷/幕/章三级纲要和一份状态快照维护长篇连续性；文学判断由虚拟作家完成，正文定稿由用户确认。本技能不负责风格报告的生成与分析（报告产出属 author-persona-skill），只把外部报告按转译映射表一次落位进人格与设定。
metadata:
  version: "0.9.0"
---

# NovelCraft

- **做什么**：把一个虚拟作家人格放进一个具体故事，用最少的文件把它写下去。
- **指导什么**：写这一章时的具体决定——这一场谁要什么、什么挡着、演到哪停；这一章必须发生什么变化；下一章动笔前要记得什么；哪些写法是这本书的敌人。
- **核心在哪**：正文。章纲 → 草稿 → 定稿是唯一产出；人格、设定、纲要、状态都只为让下一章写得更好而存在。
- **不做什么**：不生成风格报告（属 author-persona-skill），不裁决文学好坏（文学判断由虚拟作家完成），不替用户定稿，不做检查与评分。
- **工程化的边界**：只做最小约束，没有门禁。目录与命名、模板形状由 `init_project.py` 摆好，`build_release.py` 只做打包——脚本是落盘工具，不是开工门槛。

```text
人格与故事承诺 → 卷定方向 → 幕定因果 → 章前规划
→ 章纲 → 草稿 → 用户确认定稿 → 更新状态
→ 从真实结果规划下一幕
```

## 一、项目骨架

```text
state/
  author_persona.md     人格：每章开写前完整读一遍
  story_foundation.md   故事承诺、主题命题、世界观与规则、Canon、人物引擎
  current_state.md      此刻接着写需要记得什么
  memory.md             项目状态、坐标与章状态、下一步、用户偏好
workspace/
  volumes/   acts/   outlines/   drafts/   chapters/   summaries/
```

卷、幕编号两位，章三位；章号全书递增，已定稿章号不回改。模板在 `assets/templates/`：落盘时复制四份状态模板，卷纲 / 幕纲 / 章纲 / 摘要模板按需取用；字段随书自由增删——模板是起点，不是表格。

### 新项目：先看手上有多少材料

**路线一 · 已有主线或剧情大纲**——用户给了主线规划、剧情大纲或成套设定，直接展开，不回头讨论题材与主线：

1. **读人格来源**：有外部风格能力报告就完整读一遍，按 `references/style_report_mapping.md` 转译；没有就按用户描述提炼。
2. **写人格**：填 `state/author_persona.md`，不变量层写决策句，姿态层填场景表，技法层按回验规则落 3–5 张。
3. **写故事根基**：填 `state/story_foundation.md`，故事承诺六件事、主题命题、世界观与规则、Canon、主要人物引擎。
4. **写纲要**：第一卷纲、第一幕纲。
5. **落盘开工**：`python "$SKILL_ROOT/scripts/init_project.py" <项目根>` 摆好目录与模板，把 `memory.md` 项目状态改为"可写"，开始写第一章。

**路线二 · 只有一个创作灵感**——用户只有一句话、一个画面或一种情绪时，先进入讨论创作阶段，不急着落盘。这个阶段唯一该做的就是把它聊清楚：

1. **问清故事承诺**：主角想要什么、什么挡着他、失败会失去什么、读者长期想看什么、这本书在问什么。
2. **逼出主题命题**：给出与主角当前答案对立的反命题，让每一幕都在移动他的答案，而不只是移动情节（故事承诺第六件"主题张力"在这里就位）。
3. **收敛主线**：把灵感收成一条"目标 → 阻力 → 升级 → 代价"的主线。
4. **确认可写**：能说出第一卷的核心问题与不可逆变化后，再按路线一的步骤落盘开工。

一次追问两三个问题就够；用户答不上来的地方给两三个选项让他挑，不替他决定题材与主线。讨论未收敛前不建目录、不写纲要。讨论出来的故事承诺与主题命题，落盘时写进 `state/story_foundation.md`——那是它们的归宿，不要只留在对话里。

两条路线之间还有中间情况——已有题材和人物、但还没主线：按路线一开工，缺的部分当场补齐，不回头做一轮完整讨论。

### 每章

1. 读 `author_persona.md`、`current_state.md`、当前幕纲和上一章正文。
2. 写章纲到 `workspace/outlines/`。
3. 写草稿到 `workspace/drafts/`。
4. 提交用户确认；定稿后复制进 `workspace/chapters/`，更新 `current_state.md`，并把 `memory.md` 的当前坐标与章状态推进到下一章。

四步之外没有别的动作。状态写到什么程度由"下一章需要记得什么"决定，不逐字段填表。

### 修订

正文定稿后是唯一事实，只改用户明确指定的位置。

1. **受理**：列出改动会波及的后续章、现有事实、开放承诺和 Canon，给用户看，不自动改。
2. **执行**：用户确认后改正文，同步草稿与 `current_state.md`。
3. **暂停**：若触及 Canon，停止写作，交用户裁决。

### 幕末与卷末

- **幕末**：重写 `current_state.md`（仍然有效的继承，已消散的删除），写幕摘要到 `workspace/summaries/`，从真实结果规划下一幕。
- **卷末**：写卷摘要，再定下一卷。下一卷不重新套模板，先让现有角色说话——对每个活跃角色（含对手和重要配角）问四件事：卷末处在什么局面、什么还没解决、卷间反复在想什么、现在最想推动什么。主线从各角色行动方向的交汇点浮现，不是编出来的；至少两个角色的"现在想做的事"指向同一个矛盾点，主线就成立。方法见 `references/outline_craft.md` 第七节。

## 二、作家人格

`author_persona.md` 是日常写作的唯一人格输入，分两轨：

- **不变量层**（每章必读，3–6 条）：写决策句——"面对什么材料，做什么决定"。它保证跨场景仍是同一个人在决定。
- **姿态层**（按场景类型查表）：战斗 / 日常 / 仪式 / 独白 / 转场 → 句长、段落、对白占比、描写配比、禁用项。它决定"这一类场景怎么写"。

"我是谁"默认写"我是一名网络小说作者，擅长番茄小说风格，正在创作一本【题材】小说"，只把【题材】换成这本书的题材；若风格报告的平台语境明显不是番茄，把"番茄小说风格"一并换成对应平台。番茄风格按当前网文连载的执行定位校准：段落短、句子短、钩子前置、描写密度低、爽点密集、每章给出可见推进。

冲突时的优先级：`不变量层 > 红线 > 风格基调 > 姿态层与技法卡`；故事承诺高于人格。

构建规则：

1. **一次转译**：报告读完直接写进人格，不维护转接文件；采用、改写、舍弃了什么记在人格末尾。
2. **技法层简练准确**：技法卡 3–5 张，每张触发 / 写法 / 失效各一行。有外部报告时，每张必须对应报告里一张真实技法卡并标出 ID，逐条回验它的 evidence 后只写行文机制——不转述报告的 `definition` 与 `serves_purpose`，不复制证据引文；回验不过的卡舍弃。技法要真实反映报告测到的写法，而不是把报告搬过来。定量指标只作节奏参照，不做每章阈值。
3. **稳定内核，不冻结姿态**：同一位作者写战斗和葬礼会换语域，不变的是做决定的方式。

每次写章前完整重读人格文件。临时状态、章号和用户要求写进章纲或状态快照，不写进人格。人格定稿后要改，需用户批准；改动到声音，先与用户确认方向，再落笔。

## 三、设定与 Canon

只创建会影响人物选择、冲突和后果的设定。加一条设定前先问：谁的行动会被它改变？读者会在正文里感受到它吗？答不出的不写。建法与自检见 `references/setting_craft.md`。

**故事承诺**六件事：主角追求什么、根本阻力是什么、成败代价是什么、读者长期获得什么、全书核心问题是什么、主题张力落在哪。它决定纲要和人格的类型定位。

**主题命题**是取舍判据：命题、反命题、主角当前答案、本卷逼问。两个情节选项都成立时，选更能逼问主题的那个；每一幕都要移动主角的答案，不只是移动情节。

**世界观与规则**从主角第一章所在的地方往外推，不先画地图——每个地点都要在已规划的场景里用得上。势力写实际谁说了算，不写官面结构。规则分世界级 / 社会级 / 个人级三层，先问"不能做什么"再问"能做什么"；每条必须写全三要素：能做什么、不能做什么、**代价**——不写代价，读者就会问"那就用啊，为什么不用"。例外要写清谁能例外、为什么，例外本身就是剧情。

**Canon Registry** 只登记四类：身份与名称的唯一事实；不可突破的世界规则与边界；必须在唯一节点完成的结算；已定稿且改写会使前文失效的不可逆事件。每章定稿时若产生不可逆事件或新硬规则，当场登记一行（`ID｜事实｜来源章`）。Canon 与状态冲突时暂停写作，交用户裁决。

**人物引擎**至少五项：外部欲望、内在缺口、惯用方法、不愿付的代价、知识边界，再加真缺点——会让他在关键时刻做错选择的东西。它们共同回答"这个人在压力下会怎样选择"。判据：给一个意外情境，能说出他会怎么做；说不出就是履历表。配角要有自驱动目的——把主角拿走，他还有事做。需要更细时用 `references/setting_craft.md` 的认知六层模型。

## 四、规划

**卷纲**锁定方向，判据只有一条：拿着它能不能直接推出每一章该往哪走。建法见 `references/outline_craft.md`。

拆主线时，每卷是总主线的子集——指着主线问"没有这卷，主线走得完吗"，走得完就不是一卷。卷数由断点倒推，不先定几卷：卷边界是某个根本性变化（信息 / 关系 / 状态 / 事件），合格断点与不合格断点的区别在于"后续是否真的不一样了"。

展开单卷时：核心冲突必须一句话说完（谁 + 想做什么 + 被什么阻碍），对抗力量要具体，双方都要有输不起的理由；冲突阶梯 2–4 层，每层是前一层结果的直接延伸，层间转折以"删掉它后面还能按原路走吗"判定；情绪走向是弧线不是词，每一步对应阶梯的一层且必须有递进；信息差只定卷级起点→终点，终点不能等于起点，也不能完全平衡。开卷前只写第一卷。

**幕纲**以因果闭合为边界，不以章数或字数为边界：输入状态、本幕问题、策略、输出状态、幕末闭合条件、压力走向、章节因果链。因果链是**假设，不是排期**——写到哪算哪，偏离就改剩余部分；连续偏离说明本幕该重规划。

**章纲**回答四件事：

1. 本章为什么必须存在；
2. 读者的核心阅读体验是什么；
3. 场景怎样从意图推进到结果；
4. 结尾发生了什么变化，下一页为什么值得翻。

每章必须让现实、关系、人物理解或读者理解发生移动。写完自己过一遍：删掉本章，后面哪一章会坏掉？本章结束时，谁的处境与开场不同？答不出就合并、重构或删除。场景不写"经过一番""很快解决"，只写可见动作、阻力和后果。每章都要有可写的冲突载体——"本章讲信任""建立人物关系"这类没有具体事件的章纲等于没写。

## 五、状态

`current_state.md` 回答一个问题："此刻接着写需要记得什么？"四块内容，长度以"下一章真的会用到"为准：

- **当前坐标与下一章约束**：卷/幕/章、书内时间地点、眼前压力、必须承接的事、需要保持的回声、用户要求。
- **现状**：身体、关系、资源与债务、知识边界、开放承诺，以及下一章忘记就会失真的情绪、未说出口的话、动作习惯和意象。仍然有效的留着，消散的删掉。
- **读者与线索**：读者已知、怀疑、误解、期待什么；未回收的线索写清埋设章和计划回收章，该回收时别忘。
- **事件流水**：每章定稿后追加一行，自由写，记本章关键变化；登记了 Canon 就带上 ID。

每章定稿后：正文进 `chapters/`，`current_state.md` 按上面四块更新变动部分，`memory.md` 的坐标与章状态推进一章。幕末整体重写一次 `current_state.md`。需要精确事实回正文查，状态只负责"记得"。

用户要求检查前后矛盾或伏笔时，重读相关章节和状态文件逐条比对，不靠工具打分。

## 六、去AI味与收尾

去AI味不是词汇替换，是让人物和场景有眼前的事可办。开写前过一遍 `references/anti_ai.md` 的场景纪律，写完自己回看一遍清单——不用脚本，不做评分。

定稿前统一中文标点：半角双引号配对转中文引号，英文省略号转中文省略号；引号不成对就逐处确认，不做整篇替换。

## 七、工具

两个脚本都不判断、不检查、不评分：一个把目录与模板摆成约定的形状，一个打出一份干净的发布 zip。

```bash
python "$SKILL_ROOT/scripts/init_project.py" <项目根>
python "$SKILL_ROOT/scripts/build_release.py"
```

## 八、原则

1. 每章必须有不可忽略的移动。
2. 正文定稿后是唯一事实，定稿由用户确认。
3. 故事承诺和人格高于技法和指标。
4. 删掉不改变决策的规则；不引入需要维护的流程。
